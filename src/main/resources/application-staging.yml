spring:
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USER}
    password: ${DATABASE_PASSWORD}
    hikari:
      maximum-pool-size: 8
      minimum-idle: 3
  jpa:
    show-sql: false

logging:
  level:
    com.starter.app: INFO
